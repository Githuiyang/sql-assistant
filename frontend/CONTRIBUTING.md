# 贡献指南

感谢您对 SQL Assistant 项目的关注！我们欢迎任何形式的贡献。

## 🤝 如何贡献

### 报告Bug

如果您发现了bug，请：

1. 在 [Issues](https://github.com/githuiyang/sql-assistant/issues) 中搜索是否已有相关问题
2. 如果没有，创建新的 Issue，包含：
   - 清晰的标题描述问题
   - 复现问题的详细步骤
   - 预期行为 vs 实际行为
   - 截图或错误信息
   - 环境：浏览器、操作系统等

### 提出新功能

1. 先在 [Issues](https://github.com/githuiyang/sql-assistant/issues) 中讨论
2. 说明功能的用途和价值
3. 等待维护者反馈后再开始开发

### 提交代码

#### 开发环境设置

```bash
# 1. Fork 本仓库
git clone https://github.com/githuiyang/sql-assistant.git

# 2. 安装依赖
cd sql-assistant/frontend
npm install

# 3. 启动开发服务器
npm run dev

# 4. 在浏览器打开 http://localhost:3000
```

#### 代码规范

- **TypeScript**: 使用 TypeScript 进行类型检查
- **ESLint**: 遵循项目的 ESLint 规则
- **Prettier**: 使用 Prettier 格式化代码（推荐配置）
- **命名规范**:
  - 组件：PascalCase (如 `MyComponent.tsx`)
  - 工具函数：camelCase (如 `formatDate.ts`)
  - 常量：UPPER_SNAKE_CASE (如 `API_BASE_URL`)

#### Git 提交规范

使用约定式提交（Conventional Commits）：

```
<type>(<scope>): <subject>

<body>

<footer>
```

**类型 (type)**:
- `feat`: 新功能
- `fix`: Bug修复
- `docs`: 文档更新
- `style`: 代码格式（不影响功能）
- `refactor`: 重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建/工具链相关

**示例**:
```bash
feat(dictionary): 添加字段导出功能

实现了将字段字典导出为CSV格式的功能，
用户可以在字典页面点击导出按钮下载。

Closes #123
```

#### 提交 Pull Request

1. 从主分支创建特性分支：
   ```bash
   git checkout -b feature/your-feature-name
   ```

2. 编写代码并提交：
   ```bash
   git add .
   git commit -m "feat: add your feature"
   ```

3. 推送到您的 Fork：
   ```bash
   git push origin feature/your-feature-name
   ```

4. 在 GitHub 上创建 Pull Request，包含：
   - 清晰的标题和描述
   - 关联相关的 Issue
   - 截图或演示（如果适用）
   - 确认所有检查通过

## 📂 项目结构

```
frontend/
├── app/                    # Next.js App Router
│   ├── page.tsx           # 首页
│   ├── dictionary/        # 字典页面
│   ├── generate/          # SQL生成页面
│   └── api/               # API路由
├── components/             # React组件
│   ├── ui/                # 基础UI组件
│   ├── import/            # 导入相关组件
│   ├── dictionary/        # 字典相关组件
│   └── generate/          # 生成相关组件
├── lib/                    # 工具库
│   ├── db.ts              # 数据库操作
│   ├── api/               # 大模型API
│   ├── prompts/           # Prompt模板
│   └── utils.ts           # 工具函数
└── styles/                 # 样式文件
```

## 🎨 设计规范

### 配色方案
- 背景：`#0a0a0a`, `#0f0f0f`, `#141414`
- 文字：`#e8e8e8`
- 强调色：`#5a6b7a`
- 功能色：success/warning/error/info

### 组件开发
- 使用 shadcn/ui 基础组件
- 遵循清冷科技风设计
- 添加适当的动画效果（Framer Motion）
- 保持组件可复用和可组合

## 🧪 测试

```bash
# 运行测试（待添加）
npm test

# 运行 E2E 测试（待添加）
npm run test:e2e
```

## 📝 文档

- 代码注释：使用 JSDoc 格式
- README：更新主要功能和使用说明
- CHANGELOG：记录版本变更

## 🌐 国际化

目前主要支持中文，未来会添加英文支持。

## ⚖️ 行为准则

- 尊重所有贡献者
- 欢迎不同观点和建设性反馈
- 关注问题本身，而非个人

## 📧 联系方式

- GitHub Issues: [提交问题](https://github.com/githuiyang/sql-assistant/issues)
- Email: your-email@example.com

## 🙏 致谢

感谢所有贡献者的付出！

---

**有任何问题？欢迎随时提问！**
